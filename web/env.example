# Environment Variables Configuration
# These variables can be set during build time to configure the application

# API Configuration
# Base URL for the backend API
VITE_API_BASE_URL=http://localhost:8080

# API request timeout in milliseconds
VITE_API_TIMEOUT=30000

# Feature Flags
# Enable debug mode for development
VITE_DEBUG_MODE=false

# Enable cluster mode functionality
VITE_CLUSTER_MODE=true

# UI Configuration
# Default theme (light/dark)
VITE_THEME=light

# Default language (en/zh)
VITE_LANGUAGE=en

# Production Examples:
# VITE_API_BASE_URL=https://api.example.com
# VITE_API_TIMEOUT=60000
# VITE_DEBUG_MODE=false
# VITE_CLUSTER_MODE=true 

# OIDC (Frontend)
# These are optional because backend exposes /auth/config at runtime. If you need static values:
# VITE_OIDC_ISSUER=https://your-idp/.well-known/openid-configuration
# VITE_OIDC_CLIENT_ID=agentsmith-web